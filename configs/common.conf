# spec
num_replica                 4
num_faulty                  1

# replica
max_batch_size              1000
# pbft
max_num_inflight            1
# hotstuff
supply_size                 100
refill_threshold            100

# client
# supported by unreplicated, hotstuff
# open_loop                   true
# num_max_concurrent          1000
# sending_rate                1000.

# task
num_client                  10
client_duration             10. # second
tick_interval               1. # second
server_interconnect_delay   0. # second, QUIC allow connect before listen so no need

# supported by pbft
# use_tcp                     true
# server_interconnect_delay   10. # second, rather long for human reaction
